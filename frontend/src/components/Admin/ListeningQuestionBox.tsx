

type TProps = {
    children: JSX.Element
}

const ListeningQuestionBox = (props:TProps) => {
    const {children} = props
  return (
    <>
    {children}
    </>
  )
}

export default ListeningQuestionBox